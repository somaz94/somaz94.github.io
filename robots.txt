# Search engines stay fully allowed. Only AI *training* crawlers are turned
# away — see the two groups below for which is which and why.
#
# robots.txt is a request, not a control. Well-behaved crawlers honour it;
# a scraper that does not care will fetch the pages anyway. This opts out of
# the ones that publish a documented opt-out and respect it.

# --- AI training crawlers: blocked ------------------------------------------
# These collect pages to train or ground generative models. None of them
# sends readers here, so blocking them costs no traffic.
#
# Google-Extended is the one that looks alarming and is not: Google documents
# it as separate from Search. Blocking it opts the site out of Gemini / Vertex
# model training and grounding without affecting Search indexing, ranking, or
# Search Console. It is not Googlebot.

User-agent: GPTBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: anthropic-ai
Disallow: /

User-agent: Claude-Web
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: Meta-ExternalAgent
Disallow: /

User-agent: meta-externalagent
Disallow: /

User-agent: FacebookBot
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: cohere-ai
Disallow: /

User-agent: Diffbot
Disallow: /

User-agent: omgili
Disallow: /

User-agent: omgilibot
Disallow: /

User-agent: Webzio-Extended
Disallow: /

User-agent: ImagesiftBot
Disallow: /

User-agent: Timpibot
Disallow: /

# --- Deliberately NOT blocked ----------------------------------------------
# These are answer engines, not training crawlers: they fetch a page in order
# to cite it to a reader, and the citation sends that reader here. Blocking
# them would remove the site from those answers for no gain.
#
#   OAI-SearchBot   ChatGPT search citations
#   ChatGPT-User    user-initiated fetch (someone pasted a link)
#   PerplexityBot   Perplexity citations
#   Applebot        Siri / Spotlight (distinct from Applebot-Extended above)
#
# Add one here only after deciding the citation traffic is not worth it.

# --- Everyone else ----------------------------------------------------------

User-agent: *
Allow: /

Sitemap: https://somaz.blog/sitemap.xml
